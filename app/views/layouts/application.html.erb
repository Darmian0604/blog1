<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Blog" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <link rel="stylesheet" href="/assets/application.css" data-turbo-track="reload" />

  </head>

  <body>
  <div>
    <%= notice %>
  </div>

  <nav class="navbar">
    <%= link_to "Home", root_path, class: "navbar-link" %>
    <%= button_to "Log out", session_path, method: :delete, class: "navbar-button" if authenticated? %>
  </nav>

  <main class="main-content">
    <%= yield %>
  </main>

  <footer class="footer">
    <div class="footer-content">
      <p>&copy; <%= Time.now.year %> Your Company. All rights reserved.</p>
      <div class="footer-links">
       
      </div>
    </div>
  </footer>
</body>


</html>
